# UI/UXルール

自社の業務システム(買取案件管理・賃貸管理アプリ、および今後作るシステム)で共通して守るべき方針です。**②デザインシステム(design-tokens.json)がどんな値を使うかを決める前提として、なぜその値にするのかを説明するのがこの文書の役割です。** 新しい画面や新しいシステムを作る時は、まずここを読んでから②③④(トークン・共通コンポーネント・画面テンプレート)を使ってください。

## 1. 色の使い方

- **ネイビーを基調、オレンジを強調用アクセントに限定する。** アクセントカラーを増やさない。増やすほど「本当に重要な操作」が埋もれる。
- **色は意味(機能)に紐づけてから使う。** 「なんとなく綺麗だから」ではなく、「管理形態ごとに色分けする」「危険/警告/成功の状態を表す」など、色を見ただけで意味が伝わる場面だけで使う。
- **ダークモード・グラスモーフィズム(すりガラス調)は採用しない。** 理由: お金を扱う長時間のデータ入力・確認業務が中心のため、(1) 淡い背景の方が目が疲れにくい、(2) 紙のPDF明細書(白地)とトーンを合わせたい、(3) ぼかし処理は数字の視認性を落とす。派手さより正確さを優先する。
- **ロゴ本体は、常時表示ヘッダーの小さいアイコン(識別マーク)としてなら業務画面でも使う。ただし白抜き(単色)にし、ブランドカラー(スカイブルー・ピンク)そのものは業務画面に出さない。** ヘッダー以外の業務データ・操作系はネイビー/オレンジの体系を維持する。(2026-09-21改訂: 以前は業務画面でのロゴ使用を全面禁止していたが、アプリ間でヘッダーの会社名表示にばらつきがあったため、ヘッダーの白抜きロゴアイコンに限定して許可する方針に変更)
- **ロゴ画像は正方形に固定しない。高さのみ`logo.headerH`(②)を指定し、幅はauto(比率維持)にする。** ロゴ画像自体が縦長(255×320px)の実寸を持つため、`h-X w-X`のように幅を固定すると引き伸ばされて歪む。(2026-09-21追記: anken-reactでこの歪みが発生し発覚)

## 2. ナビゲーション構造

- **サイドバー(デスクトップは常時表示)＋ハンバーガーメニュー(モバイルは開閉式)を標準とする。**
- **画面内の切り替えは、セグメントコントロール(ピル型の左右切替)かアンダーラインタブを使う。** サイドバーとは別に、ページ内のサブ切替として使う。
- **レスポンシブのブレークポイントは900px。** これ未満でサイドバーを閉じてハンバーガーに切り替える。
- **例外: anken-react(買取・仲介案件管理 v2)は上部ナビ＋モバイル下タブを採用する。** トップ階層が「今日／案件／在庫」の3つだけで、頻繁にモバイルから使うため（2026-09-19、docs/REDESIGN.md §2で決定）。他アプリや同アプリの深い階層は引き続きサイドバー標準に従う。

## 3. 文字サイズ・間隔・角丸の考え方

- 具体的な数値は②(design-tokens.json)を見ること。ここでは考え方だけ書く。
- **UI要素(バッジ・ナビラベル・補助単位など、見て識別するだけの要素)の基準は13px。** 極端に小さい文字(10px未満)は使わない。
- **確認・入力する値(金額・査定額・フォームの入力値など、読み間違えると業務に影響する要素)の基準は14px。** 13pxより1段階上げ、UI要素と区別する。既存画面でこの基準より小さい値表示(例: 査定フォームの入力欄)が見つかった場合は、見つけた時点で14pxへ揃える。
- **角丸は要素の役割で決める傾向がある。** ボタン・入力欄は控えめ(8px前後)、カードはやや大きめ(12px)、モーダルはさらに大きめ(14px)、ピル型バッジは完全な丸(20px)。
- **枠線は太くしない。** 0.5pxの細い線を基本とし、かっちりした印象を避ける。

## 4. 値を揃える時の原則(最重要)

- **「近い値で近似する」を禁止する。** Tailwindの`p-8`や`rounded-lg`のような「だいたい似ている」既製クラスで済ませると、見た目は似ていても実測値はズレる。これが「何度直しても揃わない」の原因になった。
- **揃える時は実測(ブラウザのgetComputedStyle等)で正確な値を確認してから直す。** 目視だけで「似ている」と判断しない。
- **値は共有トークン(design-tokens.json)を唯一の正とする。** 片方のアプリだけで値を変えない。変えたい時はトークン側を直し、両アプリに反映する。

## 5. コンポーネントの使い方

- **新しい画面を作る時、まず共通コンポーネント(③ Button/Modal/Badge/Card/KpiRow/ListDetailLayoutなど)を探す。** なければベタ書きせず、まず共通コンポーネント側に追加してから使う。
- **同じ見た目のUIを3回以上書きそうになったら、共通コンポーネント化を検討する。**

## 6. 余白のリズム(呼吸感)

- **コンポーネント内部の間隔は引き続き`sp1`〜`sp48`を使う。** ボタン内padding、要素間gapなど、操作領域の密度はこれまで通り。
- **セクション間・ページ全体の余白・空状態(EmptyState)には`sp64`/`sp80`/`sp96`を使う。** 情報密度の高い一覧画面でも、区切りとなる箇所は広く取り「間」を感じさせる。
- **操作領域は密に、余白領域は広く、とメリハリをつける。** どちらも無闇に増減させない。Appleのインターフェースは余白そのものを一つの要素として扱い、詰め込みすぎない。それをGoogle的な情報密度・効率と両立させるための使い分け。

## 7. モーションの質

- **transitionには必ず`easing`トークンを指定する。** 指定なし(ブラウザ既定の`ease`)にしない。
- **出現・展開(モーダル/パネル/ドロップダウンが開く)には`easedec`(減速)を使う。**
- **消失・収縮(閉じる・削除)には`easeacc`(加速)を使う。**
- **色・背景・枠線などの単純な状態変化(hover等)には`easestd`(標準)を使う。**
- durationは既存の`dur1`〜`dur3`をそのまま使う(新規追加なし)。何もかも同じ直線的なイージングだと機械的な印象になるため、開閉・状態変化に応じた自然な速度変化で上質さを出す。

## 8. 技術的な注意(実装時)

- 新しいCSS変数・Tailwindのカラー/トークン名を追加する前に、既存の名前と衝突していないか確認する(過去に`--s2`という名前が色と間隔で衝突し、23箇所の背景色が壊れる事故があった)。
- コード変更後は、型チェックと実際の画面表示の両方で確認してから完了とする。

---
最終更新: 2026-09-21。この文書は買取案件管理・賃貸管理アプリのUI統一作業を通じて得られた原則をまとめたものです。新しい判断が必要になった時はここに追記していく。
